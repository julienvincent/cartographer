{
  "name": "cartographer",
  "private": true,
  "scripts": {
    "build:packages": "tsc -b",
    "build:interface": "cd interface && pnpm build",
    "build": "pnpm build:packages && pnpm build:interface",
    "postinstall": "cp ./scripts/pre-commit.sh ./.git/hooks/pre-commit",
    "start": "pnpm build:packages && cd interface && pnpm dev"
  },
  "dependencies": {
    "@types/node": "^18.11.12",
    "prettier": "^2.8.1",
    "typescript": "^4.9.4",
    "@types/jest": "^29.2.4",
    "jest": "^29.3.1",
    "ts-jest": "^29.0.3"
  }
}
