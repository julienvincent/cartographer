{
  "name": "cartographer",
  "private": true,
  "workspaces": [
    "interface",
    "pixels",
    "staircases"
  ],
  "scripts": {
    "build:packages": "tsc -b",
    "build:interface": "cd interface && yarn build",
    "build": "yarn build:packages && yarn build:interface",
    "start": "cd interface && yarn dev"
  },
  "dependencies": {
    "@types/node": "^14.14.28",
    "typescript": "^4.1.5"
  }
}
