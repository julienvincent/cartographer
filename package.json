{
  "name": "cartographer",
  "private": true,
  "workspaces": [
    "interface",
    "pixels",
    "staircases"
  ],
  "scripts": {
    "build:packages": "tsc -b",
    "build:interface": "cd interface && yarn build",
    "build": "yarn build:packages && yarn build:interface",
    "postinstall": "cp ./scripts/pre-commit.sh ./.git/hooks/pre-commit",
    "start": "yarn build:packages && cd interface && yarn dev"
  },
  "dependencies": {
    "@types/node": "^14.14.28",
    "prettier": "^2.2.1",
    "typescript": "^4.1.5"
  }
}
