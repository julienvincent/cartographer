{
  "name": "@cartographer/mapgen",
  "version": "0.0.1",
  "private": true,
  "main": "dist/index",
  "types": "dist/index",
  "scripts": {
    "build": "tsc -b",
    "test": "jest --verbose"
  },
  "dependencies": {
    "@cartographer/pixels": "^0.0.1",
    "lodash": "^4.17.20",
    "nbt-ts": "^1.3.3"
  },
  "devDependencies": {
    "@types/lodash": "^4.14.168"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "watchPathIgnorePatterns": [
      "tests/test-drafts"
    ]
  }
}
