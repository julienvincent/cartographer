/**
 * This API's scale is working in units of 'maps' and this scale factor is how to go from a scale unit of maps to
 * a scale unit of pixels (or blocks).
 *
 * For example an input scale of 1x2 maps should resolve to a pixel scale of 128x256
 */
export const SCALE_FACTOR = 128;

export const RENDER_IMAGE_MAX_SIZE = 640;
